s1 = {1, 3, 4, 9, 14, 69, 69} # sets do not allow duplicates, so the second 69 is ignored
print(s1)

s2 = set() # this is how to create an empty set, using {} will create an empty dictionary instead
print(s2)